# You can use the mathematical operators: '+' and '*' with the list.

a=[1,1]
b=[2,2]
c=a+b*3
print(c)

# List operations: Using "append" and "extend" to add list elements.
# What are the final values of 'a' and 'b' in the following operations?
a = [ ]
b = [0]
a.append([ ])
b.append([0])
a.extend([ ])
b.extend([0])
print(a,b)