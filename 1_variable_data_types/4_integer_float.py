"""Numbers that contains decimal point are called floating point numbers.
The type(x) function will return if the argument x is a float. You can use
the float(x) and int(x) function to convert values between float and integer.
"""

# Change the type of the variable x to float
# Change the type of variable y to integer
X = 123446754336788543835697
Y = 3.14159265358979323846
X = float(X)
Y = int(Y)
print(type(X),type(Y))
