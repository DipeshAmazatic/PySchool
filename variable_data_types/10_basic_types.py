"""Like other programming languages, Python also has some basic
types like numbers, strings, lists and dictionaries."""
# Assign arbitrary values to the variables such that they are of the types used in the examples
A = ""
B = 2
C = 2.1
D = []
print(type(A),type(B),type(C),type(D))
